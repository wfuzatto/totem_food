'use strict';const{app,BrowserWindow,globalShortcut}=require('electron'),path=require('path'),target=process.env.TOTEM_URL||'http://127.0.0.1:3088';function create(){const win=new BrowserWindow({fullscreen:true,kiosk:true,autoHideMenuBar:true,webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,nodeIntegration:false,sandbox:true}});win.loadURL(target);win.on('closed',()=>app.quit());for(const k of ['CommandOrControl+R','F5','F11'])globalShortcut.register(k,()=>{})}app.whenReady().then(create);app.on('window-all-closed',()=>app.quit());
